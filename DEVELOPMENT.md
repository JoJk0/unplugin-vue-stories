# Development

## Parser

- [ ] Extract props
- [ ] Extract events
- [ ] Extract slots
- [ ] Extract models
- [ ] Extract component description
- [ ] Extract Design ID
- [ ] Extract CSS properties

## Transformer

- slots: get main component's slots in story and add them to args for control
- using styles & removing ununsed classes/ids
- <Story v-for> support
- add `preview` prop to <Story> block to change default preview story
- meta get meta only when `?meta` added to component import
- v-bind args obj on story root component 
- description alias for component and story description
- support `component` prop on <Stories> and <Story> to define component for stories
- support `inheritArgs` prop on <Story> to control whether args are inherited from the parent
- reusable templates composing/nesting
- reusable templates of components that are NOT stories

args:
- v-model support

- AI skills
